# Shared settings field intent

The coordinator now provides resolve_settings_intent(values, admitted_settings, submitted_at). It merges only the seven editable input fields onto the admitted owner document, freezes missing independent currency dates using the existing owner timezone policy, and retains freight metadata and rounding mode. Existing apply_internal_settings remains the sole canonical validator and mutation path; the resolver does not calculate or write prices.

Production-first checkpoint: installed coordinator SHA256 81c13080f370cbc4d0256a09778fe8ee877c0966f4a76c3d5c736cd771c868b1. A real WordPress probe passed 12 assertions covering mixed CNY/profit, default date, explicit override, preserved USD date and freight metadata, rejected unowned metadata, and exact unchanged settings readback. Current CNY remained34500; no data mutations occurred. Apache, PHP-FPM and WebSocket services were active after worker restart.

Incomplete: async settings admission, durable explicit-field/resolved-settings storage, worker invocation and Go mixed-job routing still need wiring. Do not claim mixed writeback is unified yet. Do not split a mixed request into multiple currency/profit operations or pass unedited workbook values as explicit overrides.

Async owner wiring installed: POST /pricing/settings accepts explicit settings plus the existing quoted If-Match and Idempotency-Key boundaries. The shared queue persists submitted_settings, resolved desired_settings and desired_fields; it invokes the existing guarded apply_internal_settings and verifies explicit/generated fields against transaction publication evidence. Complete admission documents use the existing canonical normalizer without a redundant owner read. Existing currency async regression checks passed46tests/514assertions.

Real REST acceptance: request settings-owner-unchanged-20260909-01, job6eef41b514057e09ccd9f8f7421a3dd9,generation68, confirmed in2763ms with CNY34500 and profit30 unchanged. Initial probe lacked required headers and was rejected428; exact request lookup returned404/not_found before correcting headers with the same ID. Independent before/after settings matched. No changed-value worker execution or Go mixed-path acceptance is claimed yet. Live Apache/PHP-FPM/WebSocket services were active after installation.

Changed-input live worker acceptance: settings-owner-change-20260909-01 (job0308849b4e9af4d34d7c08490fe13bb0,generation69) submitted CNY34600 plus unchanged profit30 in one request. Worker confirmed in29045ms; omitted CNY date became2026-09-09. Independent arithmetic/stock audit covered all1022 mapped owners,901 priced/121 unpriced, zero errors. Restore settings-owner-restore-20260909-01 (job1572ab2e50add694cf0b98929244f7ee,generation70) confirmed in30686ms with explicit prior CNY34500/date2026-09-08. All1022 rows and complete settings exactly matched the original capture after restore; services remained active. This proves the actual mixed-field queued CNY path, not changed-profit/freight/rounding or native consumer acceptance.
